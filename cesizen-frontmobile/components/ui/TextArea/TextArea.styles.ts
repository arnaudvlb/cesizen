import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textarea: {
    width: "100%",
    minHeight: 140,

    padding: 16,
    marginTop: 16,

    borderWidth: 1,
    borderRadius: 14,

    fontSize: 15,
    lineHeight: 22,

    textAlignVertical: "top",
  },
});
